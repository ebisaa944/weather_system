services:
  - type: web
    name: weather-system
    runtime: python
    plan: starter
    buildCommand: bash build.sh
    preDeployCommand: python manage.py migrate --noinput
    startCommand: gunicorn weather_system.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --threads 2 --timeout 120
    healthCheckPath: /health/
    autoDeploy: true
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.7
      - key: DEBUG
        value: "False"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: localhost,127.0.0.1
      - key: DATABASE_URL
        fromDatabase:
          name: weather-system-db
          property: connectionString
      - key: REDIS_URL
        fromService:
          type: redis
          name: weather-system-redis
          property: connectionString
      - key: CELERY_BROKER_URL
        fromService:
          type: redis
          name: weather-system-redis
          property: connectionString
      - key: WEATHER_API_KEY
        sync: false
      - key: OPENWEATHER_API_KEY
        sync: false
      - key: EMAIL_BACKEND
        value: django.core.mail.backends.console.EmailBackend

  - type: redis
    name: weather-system-redis
    plan: starter
    ipAllowList: []

databases:
  - name: weather-system-db
    plan: basic-256mb
